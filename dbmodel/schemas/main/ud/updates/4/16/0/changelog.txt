- Add profile interpolation dynamic form.
- Optimize `vf_link` and `vf_gully` selector filters for large selector sets (same pattern as common `vf_*` views).
- Add Force reconnect checkbox to Connect Link dialog (link_to_gully).
