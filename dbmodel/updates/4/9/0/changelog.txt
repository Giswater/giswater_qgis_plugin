4.9.0
*************
- Add `inp_dscenario_pattern` and `inp_dscenario_pattern_value` tables.
- Reorder `inp_dscenario_demand` so the `id` column is the first column.
- Create new views (`v_*`) for utils tables. `v_ext_*` views no longer depend on the base table so ws/ud can point them to utils without structural coupling.
- Rename `v_ext_*` views to `ve_*` where applicable.
- Drop `expl_id` and `sector_id` from utils base tables where harmonization requires it.
- Harmonize `code` columns across utils catalogues.
- New typevalue for dscenario type: CALIBRATION.
- Refactor hydrometers: remove `rtc_hydrometer_x_connec` / `rtc_hydrometer_x_node` and add `ext_rtc_hydrometer.customer_code`.
- Add visibility tables `node_x_sector_visibility`, `node_x_municipality_visibility`, `element_x_sector_visibility`, `element_x_municipality_visibility` (ws/ud compatible usage).
- Add trigger `gw_trg_update_element_mapzones` to maintain mapzone fields on `element`.
- UD: add `has_treatment` to `node`, `connec`, and `gully`; refresh triggers/views and `ve_node`, `ve_connec`, `ve_gully`.
- Graphanalytics mapzones: compute `drainzone_outfall`, refresh `dwfzone_outfall`, add `v_recalculate_macromapzones` plumbing, fix `expl_id[]` / `muni_id[]` / `sector_id[]` assembly, skip `sector_id = 0` in sector aggregates, restrict `temp_pgr_mapzone_graph` to graphanalytics mapzones workflow, temporary element structures for graph analytics.
- Populate `addfields` / `addparam.drainzone` with aggregated graphanalytics results where defined.
- Insert/update `drainzone` from `gw_fct_graphanalytics_mapzones_v1` when processing DWF mapzones.
- `sys_table`: add `provider_config`; extend related catalogue checks.
- Add `v_type_street` and align `config_form_fields` references to street types.
- Municipality logic: better resolution when a node sits near multiple municipalities.
- CSO: add overflow mapping table used by combined sewer workflows.
- `inp_subcatchment`: add/adjust foreign keys referencing municipality (utils/tablect).
- `gw_fct_pg2epa_check_network`: fix `sector_id` and `dma_id` handling for disconnected networks.
- WS dscenario export: adjust tank parameters on inlet connections in `gw_fct_pg2epa_export` path.
- `gw_fct_cm_check_data_context`: new checks, clearer errors, QC limited to `status = 1` and `action <> 3`.
- `gw_fct_cm_setcheckproject`: handle GeoJSON result payloads.
- `fct_cm_check_progress`: add percentage of executed nodes per campaign.
- `om_check_data`: target dynamic tables via `t_tables` catalogue.
- Campaign/lot selectors: `ON CONFLICT` safety for selector tables.
- `gw_fct_getprofilevalues`: cast JSON `descript` through `text`.
- `temp_anl_arc` / `temp_anl_node`: remove redundant index statements when the index already exists.
- SQL-wide: normalize `sector_id` predicates for consistent integrity across functions.
- Network mincut SQL simplified; address autofill aligned for web and desktop callers.
- Config toolbox SQL files: revised parameters for graph class and exploitation options.
- `sys_style`: fluid type line symbology updates.
