# i18n schema manifest.
#
# Standalone translations repository. Local model stores one row per language
# (column `lang`) instead of the cloud wide-column layout.
#
# SQL: `schemas/addon/i18n/base/` + semver patches under `updates/`.

kind: i18n
engine_version: 1

substitutions: {}

phases:
  - id: load_base_schema
    type: sql_dir
    description: "Bootstrap i18n schema: init, functions, tables."
    steps:
      - { source: "schemas/addon/i18n/base/init.sql", recursive: false }
      - { source: "schemas/addon/i18n/base/fct", recursive: false }
      - { source: "schemas/addon/i18n/base/base.sql", recursive: false }
      - { source: "schemas/addon/i18n/base/tables", recursive: false }
      - { source: "schemas/addon/i18n/base/ftrg", recursive: false }

  - id: updates
    type: version_walk
    description: "Apply i18n-specific update patches."
    root: "schemas/addon/i18n/updates"
    range:
      mode: "{{ run_mode }}"

  - id: register_version
    type: sql_function
    description: "Append i18n.sys_version row."
    function: "i18n.gw_fct_admin_sys_version_register"
    payload:
      client:
        lang: "{{ locale }}"
      data:
        gwVersion: "{{ plugin_version }}"
        projectType: "I18N"
        isNewProject: "{{ register_is_new }}"
        inferParentsFromConfig: "{{ infer_parents_from_config }}"
        parentSchema: "{{ register_parent_schema }}"
        mergeAddparam: {}

profiles:
  empty:
    phases: [load_base_schema, updates, register_version]
  update:
    phases: [updates, register_version]
  update_step:
    phases: [updates, register_version]
  version_bump:
    phases: [register_version]
